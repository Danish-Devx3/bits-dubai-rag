# ---------------------------------------------------------------------------
# BITS Dubai RAG - Environment Configuration
# ---------------------------------------------------------------------------
# Copy this file to .env and set your values
# Usage: cp .env.example .env

# Your server's public IP address or domain name
# This is the IP/hostname that clients will use to connect to the backend
HOST_IP=localhost

# Example values:
# HOST_IP=192.168.1.100
# HOST_IP=13.234.56.78
# HOST_IP=your-domain.com
